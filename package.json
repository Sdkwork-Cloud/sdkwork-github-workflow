{
  "name": "@sdkwork/github-workflow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reusable GitHub Actions workflow framework for SDKWork application packaging and deployment.",
  "scripts": {
    "test": "node --test",
    "validate": "npm test && node scripts/validate-repository.mjs",
    "validate:example": "node scripts/sdkwork-workflow.mjs validate --config examples/sdkwork-clawrouter/sdkwork.workflow.json",
    "matrix:example": "node scripts/sdkwork-workflow.mjs matrix --config examples/sdkwork-clawrouter/sdkwork.workflow.json --platform all --architecture all --profile all --format all --json",
    "check:app-composition": "node ../sdkwork-specs/tools/verify-repo.mjs --root ."
  },
  "engines": {
    "node": ">=20.11.0"
  },
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  }
}
